% Authored: wrapper around the generated navigation indexes.

Every documented object is listed here in alphabetical order with a link to its
entry. These lists are generated from the same model that produces the entries
themselves, so an object cannot appear in one and not the other.

The keyword index at the end of this manual complements these lists: it is
built from \texttt{\textbackslash index} entries emitted alongside every
module, class, method, function, CLI command, and HTTP endpoint, grouped by
kind.

\input{generated/indexes}
